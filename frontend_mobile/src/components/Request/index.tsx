import { RequestShowBookBtn } from './RequestShowBookBtn';
import { RequestInfo } from './RequestInfo';
import { RequestRoot } from './RequestRoot';
import { RequestExtend } from './RequestExtend';
import { RequestCancel } from './RequestCancel';

export const Request = {
  Root: RequestRoot,
  Info: RequestInfo,
  ShowBookBtn: RequestShowBookBtn,
  Extend: RequestExtend,
  Cancel: RequestCancel,
};
