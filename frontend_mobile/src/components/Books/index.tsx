import { BookRoot } from './BookRoot';
import { BookTitle } from './BookTitle';
import { BookImage } from './BookImage';

export const Book = {
  Root: BookRoot,
  Title: BookTitle,
  Image: BookImage,
};
